/**
 * Supabase auto-generated TypeScript types.
 *
 * Replace this placeholder by running:
 *   npx supabase gen types typescript --project-id YOUR_PROJECT_ID > types/supabase.ts
 *
 * Or with the Supabase CLI linked to your project:
 *   supabase gen types typescript --linked > types/supabase.ts
 *
 * The generated `Database` type is used as the generic parameter for all
 * Supabase clients in lib/supabase/*.ts to get full type-safety on queries.
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
